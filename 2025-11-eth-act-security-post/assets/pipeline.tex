\documentclass[tikz,border=10pt]{standalone}
\usepackage{amsmath}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric}

\begin{document}
\begin{tikzpicture}[
  node distance=1.5cm and 2.5cm,
  box/.style={draw, rectangle, minimum width=2.8cm, minimum height=1cm},
  arrow/.style={->, thick},
  dottedarrow/.style={->, thick, dotted},
  every label/.style={font=\small}
]
\tikzset{
  ibox/.style = {box, draw=none, fill=none}
}

% Nodes
\node[box, align=center, yshift=-0.3cm] (asm) {RISC-V \\ Binary};
\node[box, align=center, right=of asm] (trace) {Execution \\ Trace};
\node[box, align=center, below=of trace, yshift=-0.3cm] (inputs) {Ethereum \\ Block};
\node[box, align=center, right=of trace] (poly) {Polynomials};
\node[box, align=center, right=of poly] (proof) {Proof};
\node[ibox, below=of proof] (proof_phantom) {};
\node[box, align=center, below=of poly, yshift=-0.3cm] (inputs_hash) {Block \\ Hash};
\node[box, align=center, below=of proof_phantom, yshift=-0.3cm] (verdict) {True/False};
\node[box, align=center, below=of inputs_hash] (guest_hash) {Guest Program \\ Hash};
\node[ibox, below=of inputs, left=of guest_hash] (hash_phantom) {};
\node[box, align=center, below=of asm, left=of hash_phantom] (rust) {Guest Program/\\ Ethereum STF};
% Arrows

% Dotted vertical arrow from Inputs to Trace
\draw[dottedarrow] (inputs) -- 
  node[midway, left] {execute} 
  node[midway, right] {} 
  (trace);

% Horiziontal arrow from inputs to input hash
\draw[dottedarrow] (inputs) -- 
  node[midway, left] {} 
  node[midway, below] {hash} 
  (inputs_hash);

% Horiziontal arrow from inputs to input hash
\draw[arrow] (inputs_hash) -- 
  node[midway, below] {verify} 
  node[midway, right] {} 
  (verdict);

% Vertical with split labels (Code → Binary)
\draw[dottedarrow] (rust) -- 
  node[midway, left] {compile}
  % node[midway, right] {(rustc; LLVM)} 
  (asm);

% Vertical with split labels (Code → Binary)
\draw[dottedarrow] (rust) -- 
  node[midway, below] {hash}
  % node[midway, right] {(rustc; LLVM)} 
  (guest_hash);

% Vertical with split labels (Code → Binary)
\draw[arrow] (guest_hash) -- 
  node[midway, below] {verify}
  % node[midway, right] {(rustc; LLVM)} 
  (verdict);

% Horizontal with stacked labels
\draw[arrow] (asm) -- 
  node[midway, above] {execute}
  node[midway, below] {} 
  (trace);

\draw[arrow] (trace) -- 
  node[midway, align=center, above] {generate \\ witness}
  node[midway, below] {} 
  (poly);

\draw[arrow] (poly) -- 
  node[midway, above] {prove}
  node[midway, below] {} 
  (proof);

\draw[arrow] (proof) -- 
  node[midway, left] {}
  node[midway, right] {verify} 
  (verdict);

\end{tikzpicture}
\end{document}

